#ifndef INVENTORY_H
#define INVENTORY_H

#include <windows.h>

// Asynchronously executes native C++ software & hardware inventory collection
void RunInventoryCollection(HWND hParent);

#endif // INVENTORY_H